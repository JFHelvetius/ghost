\* TLC configuration for BaudErur.tla
\* See ADR-0036 §3 for the bounds-sufficiency argument.

SPECIFICATION Spec

\* Bounded constants. See ADR-0036 §3.
CONSTANTS
    M = 2
    K = 1
    W = 3

\* Five safety invariants to check exhaustively over the reachable state
\* space. If TLC reports any as violated, the corresponding property's
\* ADR is wrong about the reference policy semantics — not the
\* implementation, the *statement*.
INVARIANTS
    INV_BAUD
    INV_ERUR
    INV_PARTITION
    INV_NO_INVENTED_CONFIDENCE
    INV_HISTORY_BOUND
